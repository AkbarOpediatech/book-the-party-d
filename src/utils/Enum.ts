export enum xRole {
  Admin = 'admin',
  Vendor = 'vendor',
  Customer = 'customer'
}

export enum xInputType {
  Text = 'text',
  Email = 'email',
  Password = 'password'
}

export enum xShowAlert {
  Success = 'success',
  Error = 'error',
  Warning = 'warning',
  Info = 'info'
}
